/* #include "raylib.h"

int main(void)
{
    InitWindow(800, 450, "raylib test");
    SetTargetFPS(60);

    while (!WindowShouldClose())
    {
        BeginDrawing();
        ClearBackground(RAYWHITE);
        DrawText("Hello, raylib!", 190, 200, 20, DARKGRAY);
        EndDrawing();
    }

    CloseWindow();
    return 0;
} */

#include <stdio.h>
#include "raylib.h"

int main(int argc, char *argv[])
{
    // Create game window
    InitWindow(640, 400, "Adventure King");

    // Create game main cycle
    return 0;
}